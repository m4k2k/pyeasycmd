# Secrets store

# The password in not required if you send only unauthenticated requests
scr_passw = "mysecretpassword123"

scr_ip_host = "easy.box"
# scr_ip_host = "192.168.1.1"

# path to base64 router certificate - or set to False to disable certificate check (dont forget to remove the line containing the path)
scr_router_pub_cert = True
scr_router_pub_cert = "easycert.cer"
